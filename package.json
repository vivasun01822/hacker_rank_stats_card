{
  "name": "hacker_rank_stats_card",
  "version": "1.0.0",
  "description": "A Node.js app to generate HackerRank stats cards.",
  "main": "api/app.js",
  "scripts": {
    "start": "node api/app.js",
    "build": "echo \"Building project...\"",
    "postinstall": "apt-get update && apt-get install -y libcairo2-dev libjpeg-dev libpango1.0-dev libgif-dev build-essential g++",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.7",
    "canvas": "^2.11.2",
    "cheerio": "^1.0.0",
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1"
  },
  "engines": {
    "node": "16.x"
  }
}