{
    "version": 2,
    "builds": [
        {
            "src": "src/api/app.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/generate-card",
            "dest": "/api/app.js"
        }
    ]
}